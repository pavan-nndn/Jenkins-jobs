pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    configFileProvider([configFile(fileId: 'serverConfig', variable: 'sConfig')]) {
                        echo $sConfig
                        echo sConfig
                     }
                }
            }
        }
    }
}
