pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'starting the build state'
              
                     echo 'ending the build state'
                }
            }
        }
    }
}
