pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    configFileProvider([configFile(fileId: 'serverConfig.json', variable: 'serverConfig')]) {
                        echo $serverConfig
                        echo serverConfig
                     }
                }
            }
        }
    }
}
