pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    configFileProvider([configFile(fileId: 'serverConfig', targetLocation:'serverConfig.json',variable: 'serverConfig')]) {
                        echo $serverConfig
                        echo serverConfig
                     }
                }
            }
        }
    }
}
