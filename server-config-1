pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'starting the build state'
                    sh 'pwd'
                     //Map servers = readJSON file: serverConfig
                     ssh(credentials: ["guxgit1"]) {
                     echo 'ssh is working'
                     }
                     //echo servers
                     echo 'ending the build state'
                     
                }
            }
        }
    }
}
